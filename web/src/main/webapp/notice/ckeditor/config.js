CKEDITOR.editorConfig = function( config ) {
	//config.plugins = 'bidi,tabletools,tab,table,dialogui,clipboard,button,toolbar,menu,dialog';
	config.versionCheck = false;
	config.resize_enable = false;
};
